machine:
  pre:
      - sudo curl -L -o /usr/bin/docker 'https://s3-external-1.amazonaws.com/circle-downloads/docker-1.9.0-circleci'
      - sudo chmod 0755 /usr/bin/docker
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t movie-api:latest . | cat
    - aws ecr get-login --region us-east-1
    - docker tag movie-api:latest 680449900020.dkr.ecr.us-east-1.amazonaws.com/movie-api:latest
    - docker push 680449900020.dkr.ecr.us-east-1.amazonaws.com/movie-api:$latest
